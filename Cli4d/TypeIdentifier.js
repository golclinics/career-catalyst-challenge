// Entry Challenge V2 - 

function TypeIdentifier(item) {
    // Checks data type of the item
let ItemType = typeof item;

// Return the type of the item
return ItemType
}

// Test trial
console.log(TypeIdentifier(45));
